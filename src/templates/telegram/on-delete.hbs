Перевод <code>{{translation.id}}</code> удален.<br />
Модератор: {{issuer.nickname}} (id{{issuer.id}})<br />
{{#if reason}}
    Причина: {{reason}}<br />
{{/if}}
Внутри было:
<pre>
    {{dump translation}}
</pre>

